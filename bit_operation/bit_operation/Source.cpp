int fullPizze = (1 << 20) - 1;
int toppings;
int main() {
	int p;
	toppings |= (1 << p);

	if (toppings & (1 << p)) {

	}

	toppings &= ~(1 << p);

	toppings ^= (1 << p);

	int a, b;
	int added = (a | b);
	int intersection = (a & b);
	int removed = (a & ~b);
	int toggled = (a ^ b);


}